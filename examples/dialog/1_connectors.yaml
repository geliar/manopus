connectors:
  slack:
    type: slackrtm
    config:
      debug: false
      token: *slack_token
      channels: *slack_channels
      bot_icon_url:
      bot_icon_emoji: *slack_bot_icon
  http:
    type: http
  cli:
    type: cli