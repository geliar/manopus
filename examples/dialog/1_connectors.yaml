connectors:
  slack:
    type: slackrtm
    config:
      debug: false
      token: *slack_token
      bot_icon_url:
      bot_icon_emoji: *slack_bot_icon
      message_types:
  http:
    type: http
  cli:
    type: cli